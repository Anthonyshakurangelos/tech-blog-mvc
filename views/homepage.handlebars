 </div>
 {{#each blogs as |blog|}}
 <div class="column is-10 is-offset-1">
  <div class="box">
  <div class="media-content">
    <div class="content">
      <p>
        <strong><a href="/blog{{blog.id}}">{{blog_title}}</a></strong><small>@{{user.username}}</small>      
        <br>
        {{desc}}
        <br>
        <small>{{format_date date_created}}</small>
        <br>
        <nav>
        <a href="/comment{{comment.comment.id}}"><button class="button" id="commentBtn" type="submit">Comment</button></a>
        </nav>
      </p>
    </div>
  </div>
</div>
</div>
    {{/each}}

  {{!-- {{#if comments}} --}}
  {{!-- <div>
{{#each comments}}
    <div class="column is-10 is-offset-1">
      <div class="box">
      <div class="media-content">
        <div class="content">
          <p>
            <strong>{{username}}user name goes here</strong>
            <br>
            {{comment_desc}}user comment goes here
            <br>
            {{!-- <small>{{format_date date_created}}</small> --}}
            {{!-- </p>
          </div>
         </div>
        </div>s
    {{/each}}
    </div>  --}}
    {{!-- {{/if}} --}}
      
       
    
 
<script src="/js/comment.js"></script>
